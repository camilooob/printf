#include "holberton.h"
/**
 * get_op_func - identify function
 * @s: funct
 *
 * Return: int
 *
 **/
int _printf(const char *format, ...)
{
    int written = 0, (*drive)(char *, va_list);
    char sp[3];

    va_list pa;

    va_start(pa, format)

        return (written);
}
